interface:
  display_name: "Logbook"
  short_description: "Record work, decisions, reviews, and evidence"
  default_prompt: "Use $logbook to record this work, what was tried, what was accepted or rejected and why, and its verification."
policy:
  allow_implicit_invocation: false
