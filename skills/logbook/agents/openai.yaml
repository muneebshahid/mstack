interface:
  display_name: "Logbook"
  short_description: "Record work, decisions, reviews, and evidence"
  default_prompt: "Use $logbook to capture or refresh records of this work, its decisions, review outcomes, and evidence."
policy:
  allow_implicit_invocation: false
