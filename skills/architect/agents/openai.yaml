interface:
  display_name: "Architect"
  short_description: "Design changes with optional independent proposals"
  default_prompt: "Use $architect to design this change and return its rationale and implementation handoff."
policy:
  allow_implicit_invocation: false
