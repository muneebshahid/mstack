interface:
  display_name: "Grill Me"
  short_description: "Sharpen an idea through rigorous questions"
  default_prompt: "Use $grill-me to question this idea until every material decision is explicit."

policy:
  allow_implicit_invocation: false
