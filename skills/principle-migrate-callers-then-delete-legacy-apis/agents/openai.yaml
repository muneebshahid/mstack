interface:
  display_name: "Principle: Migrate Callers Then Delete Legacy Apis"
  short_description: "Apply migrate callers then delete legacy apis"
  default_prompt: "Use $principle-migrate-callers-then-delete-legacy-apis to assess and guide this task."

policy:
  allow_implicit_invocation: false
