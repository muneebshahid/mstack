interface:
  display_name: "Implement"
  short_description: "Route feature, fix, refactor, or prototype work"
  default_prompt: "Use $implement to choose the right implementation mode, have the Luna worker make the code changes in small units, and verify each unit directly."

policy:
  allow_implicit_invocation: false
