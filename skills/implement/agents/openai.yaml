interface:
  display_name: "Implement"
  short_description: "Make and verify changes, delegating when useful"
  default_prompt: "Use $implement to make this change directly or with a persistent worker, inspect the result, and verify the affected behavior."

policy:
  allow_implicit_invocation: false
